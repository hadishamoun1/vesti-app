// SearchStoresPage.dart
import 'package:flutter/material.dart';

class SearchStoresPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Search Stores'),
      ),
      body: Center(
        child: Text('Search for stores here'),
      ),
    );
  }
}
